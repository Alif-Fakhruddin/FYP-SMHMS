name: mobile_app
description: "Aplikasi Pemantauan Bahaya & Keselamatan (SMHMS)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Pakej untuk permintaan HTTP ke Flask Backend API
  http: ^1.2.0

  # Ikon reka bentuk iOS / Material
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Nyatakan folder gambar/asset jika ada dalam projek anda
  # assets:
  #   - assets/images/